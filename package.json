{
  "name": "cfw-easy-html",
  "version": "0.1.7",
  "description": "An easy way to modify HTML inside a CF Worker",
  "main": "src/index.js",
  "scripts": {
    "inc": "npm version patch --no-git-tag-version && git add package.json && git commit -m \"Auto-increment version.\" && git push",
    "build": "rollup src/index.js -o dist/cfw-easy-html.js -f cjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cerulean",
  "license": "MIT",
  "devDependencies": {
    "webpack": "^5.19.0",
    "webpack-cli": "^4.4.0"
  },
  "dependencies": {
    "raw-loader": "^4.0.2"
  }
}

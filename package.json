{
  "name": "cfw-easy-html",
  "version": "0.1.6",
  "description": "An easy way to modify HTML inside a CF Worker",
  "main": "dist/cfw-easy-html.js",
  "scripts": {
    "inc": "npm version patch --no-git-tag-version && git add package.json && git commit -m \"Auto-increment version.\" && git push",
    "build": "webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cerulean",
  "license": "MIT",
  "devDependencies": {
    "webpack": "^5.19.0",
    "webpack-cli": "^4.4.0"
  },
  "dependencies": {
    "raw-loader": "^4.0.2"
  }
}
